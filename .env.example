# ===== ViVideoYouTube — пример конфигурации =====
# Скопируйте этот файл в ".env" и заполните своими значениями:
#   cp .env.example .env
# Реальные ключи получаются в Google Cloud Console → APIs & Services → Credentials.

# API Key (для публичных query-запросов к YouTube Data API)
API_KEY=ВАШ_API_KEY

# OAuth2 Client ID / Client Secret (для загрузки видео от имени пользователя)
CLIENT_ID=ВАШ_CLIENT_ID
CLIENT_SECRET=ВАШ_CLIENT_SECRET

# Путь к JSON-файлу с OAuth-клиентскими данными (installed app)
CLIENT_SECRET_FILE=client_secret_ВАШ_ИД_КЛИЕНТА.json

# Файл, куда будет сохранён refresh-токен после первой OAuth-авторизации
OAUTH_TOKEN_FILE=token.json

# Скоупы для YouTube Data API
YOUTUBE_SCOPES=openid,https://www.googleapis.com/auth/youtube.upload,https://www.googleapis.com/auth/youtube.force-ssl,https://www.googleapis.com/auth/youtube.readonly

# Категория по умолчанию для видео / шортсов
# 0 = Unspecified, 22 = People & Blogs
DEFAULT_CATEGORY_ID=22

# ===== Шаблон авто-метаданных для шортсов (опционально) =====
# Эти значения подставляются в описание шортса, который генерируется
# вкладкой «Шортсы». Если оставить пустыми — строки просто не появятся.
# Задайте здесь свой сайт и телеграм-канал вместо примера ниже.
SHORTS_SITE_URL=
SHORTS_TG_URL=

# Путь к файлу-шаблону со всеми строками шортса (полная настройка).
# Пустой = используется встроенный шаблон (ниже SHORTS_BASE_TAGS).
# Пример: скопируйте shorts_template.txt.example -> shorts_template.txt
# и отредактируйте секции TITLE / DESCRIPTION / TAGS.
SHORTS_TEMPLATE_FILE=shorts_template.txt

# Базовый набор тегов, добавляемый в каждый шортс (через запятую).
# Используется, когда SHORTS_TEMPLATE_FILE пуст.
SHORTS_BASE_TAGS=аниме,анимешортс,аниме нарезка,аниме моменты,аниме эдиты,аниме 60 секунд,shorts,anime,anime shorts
